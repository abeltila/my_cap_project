part of 'index.dart';

//static colors config files to list all of the colors that the app uses
class AppColorsConfig {
  // Primary Colors
  static const Color primary = Color(0xFF2b2b2b);  // Example: Purple
  static const Color primaryDark = Color(0xFF0048B3);
  static const Color primaryLight = Color(0xFFBB86FC);

  // Secondary Colors
  static const Color secondary = Color(0xFF03DAC6);  // Example: Teal
  static const Color secondaryDark = Color(0xFF018786);
  static const Color secondaryLight = Color(0xFFB2F5EA);

  // Background Colors
  static const Color background = Color(0xFFF5F5F5); // Light gray background
  static const Color surface = Color(0xFFFFFFFF);    // White surface

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);  // Dark gray
  static const Color textSecondary = Color(0xFF757575); // Medium gray

  // Accent Colors
  static const Color accent = Color(0xFFFF5722); // Orange accent
  static const Color accentLight = Color(0xFFFF8A65);
  static const Color accentDark = Color(0xFFD84315);

  // Error Color
  static const Color error = Color(0xFFB00020);  // Red error color
}
